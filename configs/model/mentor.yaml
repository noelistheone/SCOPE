model: MENTOR

embedding_size: 64
feat_embed_dim: 64
n_layers: 2
n_ui_layers: 2
knn_k: 10
align_weight: 0.01          # cross-modal alignment loss weight (was 0.1; that was too dominant)
ssl_weight: 0.01            # InfoNCE SSL weight (was 0.1)
ssl_temp: 0.2
reg_weight: 1.0e-4
learning_rate: 0.0001
train_batch_size: 2048
