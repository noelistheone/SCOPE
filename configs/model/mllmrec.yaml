model: MLLMRec

embedding_size: 64
feat_embed_dim: 64

n_ii_layers: 1
n_ui_layers: 2

knn_k: 10
knn_jac: 10        # Baby default per MLLMRec paper
pure: 0.7          # Baby default

learning_rate: 0.001
train_batch_size: 2048
reg_weight: 0.0    # MLLMRec uses no L2 reg
