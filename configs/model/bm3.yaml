model: BM3

embedding_size: 64
feat_embed_dim: 64
n_layers: 2
dropout: 0.5
cl_weight: 2.0              # weight of contrastive (alignment) loss
reg_weight: 0.1
learning_rate: 0.001
train_batch_size: 2048
